import pandp.dat
def take_count(str):
    log = dict()
    book = str.split()

    for word in book:
        if word in log:
            log[word] += 1
        else:
            log[word] = 1
    print log

take_count(pandp)
